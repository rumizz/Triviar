# Triviar

### File structure

`client`:

- `create-react-app`
- has its own `node_modules`

`server`:

- `trpc`
- express on port 3600
- websocket on port 3001

### Setup

- `yarn client`
- `yarn server`
